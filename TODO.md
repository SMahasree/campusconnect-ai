# TODO

- [ ] Update `frontend/src/pages/Matches.js` to use the “Finder/Matchmaker hub” layout (hero + filters panel + matches list + refined cards).
- [ ] Extend `frontend/src/styles.css` with hub-specific CSS classes and replace inline styling in Matches page with classes.
- [ ] Run frontend (`npm start` in `frontend/`) and verify `/matches` UI renders correctly.

